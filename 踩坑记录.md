# 踩坑记录

> 记录本项目硬件设计与文档维护中踩过的坑。每条包含：**现象 → 原因 → 教训/规避方法**。
> 新增踩坑请按时间倒序补充，并同步更新[版本与变更记录](版本与变更记录.md)。

## 硬件装配类

### 坑 1：NRF24L01 模块引脚电源顺序与本板插座相反（⚠️ 会烧模块）

- **现象**：市面上常见 NRF24L01 成品模块排针为 Pin1=GND、Pin2=VCC；而本板插座按网表为 **Pin1=3.3V、Pin2=GND**，两者正好相反。
- **实测结论（2026-08-15）**：两块板子的 NRF 插座电源顺序不同——**飞控板（MiniFC）与市售主流一致**（Pin1=GND、Pin2=VCC，直插市售模块）；**遥控板为自定义序**（Pin1=3.3V、Pin2=GND）。信号六脚（CE/CSN/SCK/MOSI/MISO/IRQ）两块板均为 3~8 脚同序，仅电源两脚相反。
- **后果**：凭"常识"直接把市售模块插入**遥控板**会把电源反接，烧毁模块；同一模块在两块板间换插时极易顺手直插出错。
- **规避**：装配前用万用表核对模块 Pin1/Pin2 的网络归属；遥控板用**杜邦线交叉**（板座 1↔模块 2、板座 2↔模块 1，3~8 直连）或选购反序模块；建议遥控板标注"NRF 电源反序！"防止换板误插。详见[装配指南](装配指南.md)与[无线通信电路](硬件设计/电路原理图/无线通信电路.md)。

### 坑 2：摇杆按键读不到——设计上就没接 MCU

- **现象**：固件里怎么读摇杆按键都没反应。
- **原因**：JOY1/JOY2（RKJXV1224005）的按键引脚在本板上直接接 GND，未接入任何 MCU 引脚。
- **规避**：当前版本不要为按键写代码；如需按键功能，下一版硬件把按键引脚引至 MCU GPIO 并配置上拉。详见[输入接口电路](硬件设计/电路原理图/输入接口电路.md)。

### 坑 3：电池无防反接保护

- **现象**：板上没有防反接二极管/MOSFET。
- **后果**：J1 接反即可能损坏后级电路。
- **规避**：接线时严格核对极性；建议电池线用防反插连接器。改版时可考虑加 P-MOS 防反接。

### 坑 4：L1 是电感不是二极管

- **现象**：早期文档把 L1 描述为"防反接二极管"，引起误判。
- **真相**：网表确认 L1 为 **5.9µH 直插电感**，接在 VBAT_SW 与 U2 第5脚 SW（$1N192）之间，是 U2（PW5100-50 升压）电路的储能电感（v1.5 以型号定案）。
- **规避**：器件属性一律以网表为准，不凭文档描述下结论。

## 文档维护类

### 坑 5：文档与网表不一致——拓扑张冠李戴

- **现象**：多份文档曾把 U1/U2 的电源角色写反，摇杆供电电压也互相矛盾。
- **原因**：文档生成后未与网表交叉校验。
- **教训**：2026-08-10 已按网表全面修正。今后任何电路描述变更，先查网表 `Netlist_遥控器原理图_2026-08-10.tel`。

### 坑 6：Gerber 指令释义写错

- **现象**：D03 被写成"停止"（实为闪光 Flash）、G54 被写成"选择字体"（实为选择光圈的旧式前缀）、G75 被写成"子程序调用"（实为多象限圆弧模式）。
- **教训**：规范类文档的技术释义要对照 RS-274X 标准核实。已于 2026-08-10 修正，见[Gerber文件规范](制造文件/Gerber文件规范.md)。

### 坑 7：引用的源数据文件缺失

- **现象**：各文档引用的 Gerber、飞针 JSON、IPC-356A 等源文件不在资料库中，链接全部失效。
- **规避**：补入源文件后链接才可用；核对数据以网表为第一优先（见[资料总览](资料总览.md)的数据契约）。

### 坑 8：U1/U2 器件定性三次反复（v1.5 以型号定案）

- **现象**：U2 的定性经历了三次翻转：初版文档称 LDO → v1.1 按网表推断"U2 升压/U1 降压" → v1.4 经设计方确认改为"两级 LDO" → v1.5 设计方给出型号 **PW5100-50** 并确认第5脚为 SW，最终定案为"U2 同步升压 + U1 LDO"。
- **真相**：U2 = PW5100-50 PFM 同步升压（SOT-23-5，固定输出 5.0V，输入 0.7~5.0V），第5脚 = SW 开关节点，储能电感 L1（5.9µH）接于 VBAT_SW 与第5脚之间；U1 = 3.3V LDO（引脚序与网表完全吻合）；电池为单节锂电（约 3.0~4.2V）。U2 引脚经设计方逐脚确认：**1脚=EN（接 VBAT_SW，恒使能）、2脚=VOUT(5V)、3脚=NC、4脚=GND、5脚=SW**——PW5100 无独立 VIN 脚，输入功率经 L1 由 SW 脚进入，切勿把 1脚误标为 VIN。
- **教训**：① 器件定性以"**型号 + 数据手册**"为最终依据，网表连接关系只能作佐证——L1 接第5脚这一现象，用"SW 引脚"解释远比"LDO 输出脚接电感"合理，当时应更早向设计方索要型号；② 重大定性结论落库前先与实物/BOM 核对一次，避免全库反复修正的成本。

## 通用经验

1. **网表是唯一权威**：文档 ＞ 猜测永远不成立，数据契约优先级见[资料总览](资料总览.md)。
2. **上电前必做静态检查**：先测短路、核对极性，再通电。
3. **任何"标准引脚定义"都要实测复核**：模块厂商的排针顺序没有统一标准。

## 相关文档

- [调试指南](调试指南.md)（按步骤调试流程）
- [版本与变更记录](版本与变更记录.md)（修正历史）
- [资料总览](资料总览.md)（入口索引）
